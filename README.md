# VibeRunner

基于容器的现代化开发环境管理工具，为前端和全栈开发提供一致、高效的开发环境。

## 🎯 项目特点

- **统一开发环境**：基于 Docker/Podman 容器，确保团队开发环境一致性
- **快速部署**：支持本地构建或直接拉取云端镜像
- **完整工具链**：内置 Node.js 24、pnpm、opencode-ai 等开发工具
- **SSH 远程连接**：支持通过 IDE 的 Remote SSH 功能连接容器
- **代码目录挂载**：自动挂载本地代码目录到容器中
- **跨平台兼容**：适用于 Windows、macOS 和 Linux 系统

## 📋 技术栈

| 技术/组件 | 版本 | 用途 |
|---------|------|------|
| Ubuntu  | 24.04 | 容器基础系统 |
| Node.js | 24    | JavaScript 运行时 |
| pnpm    | 最新版 | 包管理器 |
| OpenSSH | 系统默认 | 远程连接服务 |
| Docker/Podman | 支持 | 容器运行时 |
| opencode-ai | 最新版 | AI 辅助开发工具 |

## 🚀 快速开始

### 前置要求

- 安装 [Podman](https://podman.io/getting-started/installation) 或 Docker
- 安装 PowerShell（Windows 系统默认已安装）

### 方法一：使用云端镜像（推荐）

1. **克隆项目**

```bash
git clone https://github.com/yourusername/VibeRunner.git
cd VibeRunner
```

2. **启动云端镜像**

```powershell
# 默认使用 ghcr.io/tsanfer/viberunner 镜像，端口 2222
./start-viberunner-cloud.ps1

# 或指定自定义镜像和端口
./start-viberunner-cloud.ps1 -Port 2223
```

### 方法二：本地构建镜像

1. **克隆项目**

```bash
git clone https://github.com/Tsanfer/VibeRunner.git
cd VibeRunner
```

2. **构建并启动**

```powershell
# 默认使用 Ubuntu 24.04、Node.js 24，端口 2222
./setup.ps1

# 或指定自定义版本和端口
./setup.ps1 -UbuntuVer 24.04 -NodeVer 24 -Port 2223
```

## 🔧 IDE 连接配置

启动成功后，脚本会输出连接信息，按照以下步骤配置你的 IDE：

1. **打开 VS Code**
2. **安装 Remote - SSH 扩展**
3. **按 F1 键**，输入并选择 `Remote-SSH: Connect to Host...`
4. **输入连接命令**：
   ```
   ssh root@localhost -p 2222 -i "C:\Users\YourUsername\.ssh\id_rsa"
   ```
5. **连接成功后**，在 VS Code 中打开 `/workspace` 目录

## 📁 目录结构

```
VibeRunner/
├── .github/workflows/     # GitHub Actions 工作流
│   └── docker-publish.yml  # Docker 镜像发布配置
├── .dockerignore           # Docker 构建忽略文件
├── Dockerfile              # 容器镜像构建文件
├── LICENSE                 # 许可证文件
├── README.md               # 项目说明文档
├── setup.ps1               # 本地构建和启动脚本
└── start-viberunner-cloud.ps1  # 云端镜像启动脚本
```

## 🔄 工作原理

1. **镜像构建**：基于 Ubuntu 24.04 构建，安装 Node.js 24、pnpm 和必要的开发工具
2. **容器启动**：启动容器并挂载本地代码目录到 `/workspace`
3. **SSH 配置**：自动配置 SSH 公钥，允许远程连接
4. **开发环境**：通过 SSH 连接到容器，使用容器内的开发工具进行开发

## 🎮 命令参考

### 本地构建命令

```powershell
# 基本构建
./setup.ps1

# 自定义配置
./setup.ps1 -UbuntuVer 24.04 -NodeVer 24 -Port 2223
```

### 云端镜像命令

```powershell
# 使用默认云端镜像
./start-viberunner-cloud.ps1

# 自定义端口
./start-viberunner-cloud.ps1 -Port 2223
```

## 🌐 云端镜像

项目提供预构建的云端镜像，可直接拉取使用：

```bash
# 拉取最新版本
podman pull ghcr.io/tsanfer/viberunner
```

## 🔒 安全说明

- 容器默认使用 root 用户运行，仅用于开发环境
- SSH 密钥用于容器内认证，请妥善保管你的私钥
- 建议在本地网络环境中使用，避免暴露公网

## 🤝 贡献指南

1. **Fork 本项目**
2. **创建特性分支** (`git checkout -b feature/amazing-feature`)
3. **提交更改** (`git commit -m 'Add some amazing feature'`)
4. **推送到分支** (`git push origin feature/amazing-feature`)
5. **打开 Pull Request**

## 📄 许可证

本项目采用 MIT 许可证 - 详见 [LICENSE](LICENSE) 文件

## 📧 联系方式

- 项目链接：[https://github.com/Tsanfer/VibeRunner](https://github.com/Tsanfer/VibeRunner)
- 维护者：Tsanfer

---

**享受编码的乐趣！** 🎉